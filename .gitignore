# Secrets -- never commit
*.env
.env

# NOTE: state.db is intentionally NOT ignored -- the workflow commits it back
# every run as the persistent cross-week dedup store (see README architecture).

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local Claude Code tool settings (not part of the project)
.claude/
