{"uid": "arxiv:2607.25162", "title": "Quantum Transformer BSDE Solver via Multi-Layer Fully-Connected Variational Quantum Circuits", "n": 11, "p": [{"s": "I. INTRODUCTION", "t": "Solving high-dimensional parabolic partial differential equations (PDEs) is important in engineering, physics, and stochastic control. Classical grid-based methods suffer from the curse of dimensionality, while Deep BSDE methods [1] [2] reformulate semilinear PDEs as backward stochastic differential equations (BSDE) [3] [4]. This gives a model-based reinforcement learning viewpoint: trajectories are generated from known stochastic dynamics, and the trainable model learns the gradient-related control process. The main learning task is therefore to approximate the map X tn \u2192 Z tn , which drives the backward propagation of the PDE solution. Variational quantum circuits (VQCs) are widely used trainable models in quantum machine learning, where classical data are encoded into quantum states, processed by parameterized gates, and read out through measurement expectations [5] [6]. However, VQC design faces an expressivity-trainability tradeoff: shallow circuits may lack capacity, while wider or deeper circuits increase simulation cost and barren-plateau risk [7] [8]. A previous quantum BSDE solver used VQC-based maps to approximate the BSDE gradient process Z tn without trainable classical neural-network layers [9]. FC-VQC extends this idea by partitioning high-dimensional inputs into local VQC blocks and propagating information through deterministic shiftedring mixing [10]. This modularity is especially useful for Transformer-style models: larger hidden dimensions d model can be obtained by adding local VQC blocks, rather than increasing the qubit count of a monolithic VQC."}, {"s": "I. INTRODUCTION", "t": "Recent quantum Transformer studies [11] include nearterm PQC-based hybrid models [12], where quantum circuits generate Transformer representations while attention remains classical. Related work on Quantum Adaptive Self-Attention further highlights the importance of capacity-matched classical controls when assessing quantum Transformer gains [13]. We introduce a Quantum Transformer BSDE solver based on FC-VQC. The key idea is to replace every trainable map in a Transformer [14] with local FC-VQC modules. Thus, all trainable model parameters are contained within the FC-VQC modules, while attention, softmax, causal masking, residual connections, concatenation, and reshaping remain classical and parameter-free. Our contributions are: (i) causal selfattention for time-space BSDE trajectory modelling; (ii) a QTransformer whose trainable model parameters are confined to scalable FC-VQC modules; and (iii) an evaluation against classical and quantum baselines at multiple hidden widths on Black-Scholes, Burgers-type, and oscillatory reactiondiffusion PDEs. II. QUANTUM TRANSFORMER BSDE SOLVER We present the proposed solver in three steps: the BSDE representation of semilinear parabolic PDEs, the FC-VQC building block, and the FC-VQC-based Quantum Transformer architecture for learning Z tn ."}, {"s": "A. BSDE Reformulation", "t": "We consider a semilinear parabolic PDE on [0, T ] \u00d7 R d for the unknown solution u : [0, T ] \u00d7 R d \u2192 R: where Lu = \u00b5(t, x) \u22a4 \u2207 x u + 1 2 Tr(\u03c3(t, x)\u03c3(t, x) \u22a4 \u2207 2 x u) is the infinitesimal generator, T is the terminal time, d is the spatial dimension, \u00b5(t, x) \u2208 R d is the drift, \u03c3(t, x) \u2208 R d\u00d7d is the diffusion matrix, and f : [0, T ] \u00d7 R d \u00d7 R \u00d7 R d \u2192 R is the driver. By the nonlinear Feynman-Kac formula, (1) admits the following BSDE representation: where Y t = u(t, X t ) and Z t = \u03c3(t, X t ) \u22a4 \u2207 x u(t, X t ). This gives a model-based reinforcement learning viewpoint: the forward SDE is the known transition model, and Z tn acts as a policy-like control in the backward value recursion. After discretizing 0 = t 0 < \u2022 \u2022 \u2022 < t N = T , the solver simulates X tn by Euler-Maruyama and propagates Quantum Transformer predicts the full gradient trajectory where Xtn denotes the normalized model input. The trainable parameters \u0398, together with the initial scalar Y 0 = y 0 , are optimized by the terminal loss"}, {"s": "B. FC-VQC Architecture", "t": "We use FC-VQC to replace dense trainable maps. Instead of encoding a d-dimensional vector into one d-qubit circuit, FC-VQC partitions the input into fixed-size local VQC blocks. For x \u2208 R d , we zero-pad the input, if necessary, to d = Bq, where q is the number of qubits per local block and B is the number of blocks: Each block is processed by an independent local VQC f \u03d5 (\u2113) b : R q \u2192 R q , consisting of rotation encoding, K parameterized entangling layers, and Pauli-Z expectation measurements. At FC-VQC layer \u2113, parameter-free shifted-ring mixing is first applied across neighbouring blocks, and the mixed block is then passed through a local VQC: For the q = 3 blocks used in our BSDE/PDE experiments, the shifted-ring mixer can be written, with cyclic block indices as Increasing the width therefore adds local VQC blocks rather than qubits per circuit, avoiding a monolithic d model -qubit VQC whose simulation cost grows exponentially with d model . Stacking these layers gives a nonlinear dimensionpreserving map whose trainable parameters scale linearly with the input dimension. We use an FC-VQC reducer with onequbit readout: each local q = 3 VQC block outputs one scalar, and the resulting features are further reduced to the target dimension. See [10] for the full FC-VQC construction."}, {"s": "C. FC-VQC-Based Quantum Transformer", "t": "For one simulated path, let X = ( Xt0 , . . . , Xt N -1 ) \u2208 R N \u00d7d be the normalized state trajectory. The model learns the pathwise map Z = T \u0398 ( X), T \u0398 : R N \u00d7d \u2192 R N \u00d7d , where Z is used in the discrete BSDE recursion. Each scalar component X(i) tn is treated as a time-coordinate token. It is repeated to d model dimensions and passed through a width-preserving FC-VQC embedding: where e time n is an optional quantum-generated time embedding and \u03b7 \u2208 {0, 1} indicates whether it is used. No coordinate or asset embedding is used in the BSDE solver. The N \u00d7d tokens are then flattened into a sequence of length S = N d. For Transformer layer \u2113 = 0, . . . , L -1, FC-VQC modules generate the query, key, and value representations: (9) Consequently, a larger Transformer hidden size is realised by more small FC-VQC blocks, whereas a monolithic quantum Transformer would require larger d model -qubit circuits in each trainable map. The representations are split into n h heads with d h = d model /n h . For each head, we apply scaled dot-product attention: The mask M is causal in time, so each token can attend only to tokens from the current and previous time steps, preserving the adapted structure of the BSDE. The head outputs are concatenated,"}, {"s": "C. FC-VQC-Based Quantum Transformer", "t": "h , projected by an FC-VQC module F \u03b8 (\u2113) O , and combined with a single FC-VQC residual feed-forward F \u03b8 (\u2113) ff : After the final Transformer layer, a VQC-based decoder maps each token representation back to one scalar: In the implementation, D \u03b8 dec is realized by a width-preserving FC-VQC module followed by an FC-VQC dimensionreduction step with one-qubit local readouts. Thus, all trainable parameters are contained within the FC-VQC embedding, QKV and output-projection, feed-forward, and decoder modules, while the attention and structural operations remain classical and parameter-free."}, {"s": "A. Benchmarks and Protocol", "t": "We evaluate on three d = 36 parabolic PDE benchmarks, written as task-specific instances of (1)-(2). Let D x = diag(x 1 , . . . , x d ) and let 1 \u2208 R d denote the all-ones vector. 1) Black-Scholes PDE/BSDE: Here \u03c3 BS is the constant volatility shared by all assets, and g port denotes the terminal portfolio payoff. 2) Burgers-type PDE/BSDE: 3) Oscillatory reaction-diffusion PDE/BSDE: where m(t, x, y) = min{1, [y -\u03ba -1 -a(t, x)] 2 }, a(t, x) = sin(\u03bb1 \u22a4 x) exp \u03bb 2 d(t-T ) 2 , \u03ba = 0.6, \u03bb = d -1/2 . TABLE I MEAN TRAJECTORY RELMAE ON THE THREE BENCHMARK PDES. LOWER IS BETTER. L DENOTES MODEL LAYERS. THE HIDDEN DIMENSION dm = d model APPLIES ONLY TO TRANSFORMER AND QTRANSFORMER. Task L DNN FC-VQC Transformer QTransformer dm = 3 dm = 9 dm = 3 dm = 9 BS 3 0.0246 0.0226 0.0148 0.0104 0.0148 0.0140 5 0.0264 0.0235 0.0230 0.0099 0.0146 0.0136 7 0.0265 0.0245 0.0271 0.0108 0.0142 0.0132 Burg. 3 0.3402 0.3463 0.3392 0.2181 0.2933 0.2620 5 0.3451 0.3309 0.4759 0.2200 0.2728 0.2617 7 0.3576 0.3324 0.4747 0.2201 0.3067 0.2566 Osc. 3 0.0876 0.0640 0.0486 0.0164 0.0300 0.0330 5 0.0720 0.0610 0.0486 0.0486 0.0318 0.0265 7 0.0910 0.0588 0.0486 0.0485 0.0272 0.0255 For evaluation, the analytical references are u BS (t, x) is the closed-form Black-Scholes formula, u B (t, x) = \u03c8(t, x) and u O (t, x) = 1 + \u03ba + a(t, x)."}, {"s": "A. Benchmarks and Protocol", "t": "All models use the same Euler-Maruyama paths, terminal condition, and terminal BSDE loss, with T = 1, N = 10, 1,000 Monte Carlo paths, batch size 128, and 10,000 epochs. We train with Adam using learning rate 5 \u00d7 10 -3 and report averages over seeds {42, 123, 456}. We sweep L \u2208 {3, 5, 7}, where L denotes model layers for DNN/FC-VQC and Transformer blocks for Transformer/QTransformer. For FC-VQC, the local VQC depth is set to K = L; for QTransformer, all internal FC-VQC modules use L VQC = 3 and K = 3. Accuracy is measured by RelMAE = 1"}, {"s": "B. Results", "t": "Table I reports the mean trajectory RelMAE, while Fig. 2 shows the corresponding time-dependent errors with one standard deviation across random seeds. The best-performing configurations exhibit the overall hierarchy: Transformer (d m = 9) > QTransformer (d m = 9) > QTransformer (d m = 3) > Transformer (d m = 3) > FC-VQC > DNN, where > denotes lower RelMAE. Overall, increasing the hidden dimension improves both Transformer architectures, with the classical Transformer at d m = 9 achieving the lowest errors on most benchmarks. QTransformer consistently improves over the non-attentive FC-VQC baseline, demonstrating the benefit of causal selfattention for learning BSDE gradient trajectories. At the compact hidden width d m = 3, QTransformer also consistently outperforms the classical Transformer. To ensure a fair comparison, we note that at L = 3, QTransformer utilizes only 2,925 trainable parameters compared to the classical Transformer's 5,717. This confirms the quantum model's parameterefficiency advantage is genuine and not an artifact of an underparameterized classical baseline. Together with the stronger performance of the d m = 9 models, these results indicate that both attention and hidden-width scaling contribute to improved accuracy."}, {"s": "C. Ablation Study", "t": "Table II reports an ablation replacing each FC-VQC map inside QTransformer with a monolithic VQC map, keeping TABLE II QTRANSFORMER ABLATION COMPARING MONOLITHIC VQC AND FC-VQC TRAINABLE MAPS WITH dm = d model = 3. HERE L DENOTES THE NUMBER OF TRANSFORMER LAYERS. ENTRIES ARE MEAN TRAJECTORY RELMAE; LOWER IS BETTER. Task L Monolithic VQC FC-VQC dm = 3 dm = 3 Black-Scholes 3 0.0145 0.0148 5 0.0144 0.0146 7 0.0153 0.0142 Burgers 3 0.3914 0.2933 5 0.3319 0.2728 7 0.3432 0.3067 Oscillating 3 0.0355 0.0300 5 0.0365 0.0318 7 0.0286 0.0272 the structure and training protocol unchanged. The FC-VQC version consistently outperforms this monolithic baseline. Furthermore, for larger d m , the monolithic variant requires impractical d m -qubit circuits per map. In contrast, FC-VQC scales by adding local blocks while keeping individual circuits small, validating both the accuracy and scalability of the design."}, {"s": "IV. CONCLUSION", "t": "We proposed an FC-VQC-based Quantum Transformer BSDE solver for high-dimensional parabolic PDEs. The method uses time-coordinate tokens and causal self-attention to learn the adapted BSDE gradient process. All trainable model parameters are contained within the FC-VQC modules, while attention and structural operations remain classical and parameter-free. Across the benchmarks, the results show the overall hierarchy Transformer (d m = 9) > QTransformer (d m = 9) > QTransformer (d m = 3) > Transformer (d m = 3) > FC-VQC > DNN, where > denotes lower RelMAE. QTransformer consistently improves over FC-VQC and outperforms the classical Transformer at d m = 3, highlighting the benefit of causal attention at compact hidden width. Increasing d m improves both Transformer architectures, with the classical Transformer at d m = 9 performing best overall. The ablation study further shows that FC-VQC trainable maps can improve QTransformer performance compared with monolithic VQC maps. Beyond accuracy, FC-VQC provides a practical scaling advantage: larger d m can be obtained by adding local VQC blocks, whereas the monolithic variant requires d m -qubit circuits that may become impractical for larger hidden dimensions. These initial results rely on ideal simulations and three random seeds. Future work will incorporate statistical significance testing across more seeds, evaluate finite-shot measurement and hardware noise impacts, and empirically measure gradient variances to substantiate the qualitative barren-plateau mitigation claim. We will also extend models to larger hidden dimensions under strictly matched-parameter budgets to further isolate genuine quantum inductive biases."}]}