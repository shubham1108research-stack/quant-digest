# PINNED EXACTLY, on purpose.
#
# Every workflow here runs unattended on a schedule -- the digest at 04:30 UTC,
# backfills overnight. With floors (requests>=2.31) a breaking release lands in
# a 3am run nobody is watching and the first symptom is a missing digest, or
# worse, a run that half-succeeds and commits the result. A pin means the
# version changes when I change it, in a commit, with a run I am looking at.
#
# Bump deliberately: raise one, run the digest with force=true, read the log.
requests==2.34.2
feedparser==6.0.12
playwright==1.60.0   # headless-browser scrape of AQR/Man/RA house research
boto3==1.43.78       # Cloudflare R2 (S3 API) -- tools/dbsync.py
numpy==2.5.2         # kNN graph, PCA/k-means map (tools/graph.py, map.py, embed_ab.py)
# LLM triage and embeddings both use plain REST via requests -- no SDK, so no
# extra dependency and no SDK-version surprises in an unattended run.
esprima==4.0.1
